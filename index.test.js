const server = require('./index.js')

describe('index.js', () => {
  it('runs the test', () => {
    expect(true).toBeTruthy();
  });
  it('runs the test', () => {
    expect(true).toBeTruthy();
  });
});  